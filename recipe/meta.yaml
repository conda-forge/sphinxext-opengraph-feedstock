{% set name = "sphinxext-opengraph" %}
{% set version = "0.9.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sphinxext-opengraph-{{ version }}.tar.gz
  sha256: 4e57e25b6d56f47b9c06a5a5d68a2a00ed3577c8a39e459b52118c6bfe5e8c8b

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools_scm
  run:
    # Not in setup.py, but referenced in code (automatically installed by Sphinx)
    - docutils
    - python >=3.7
    - matplotlib-base
    - sphinx >=4.0

test:
  imports:
    - sphinxext.opengraph
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/wpilibsuite/sphinxext-opengraph
  summary: Sphinx Extension to enable OGP support
  license: BSD-3-Clause
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - manics
